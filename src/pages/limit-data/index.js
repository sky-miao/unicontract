import React from 'react';
import './index.less';
import {
 connect
} from 'react-redux'
import { Button, } from 'antd';
export default class DataPage extends React.Component {
 render(){
     return (
       <Button>账户审核</Button>
     )
 }
}
